/**
 * TODO(phase3): LLM "city concierge" chat panel.
 * Planned: a side panel that answers questions about the visible area by
 * composing parcel / crime / history / transit layer queries. Requires an
 * LLM API key, so it stays a stub while the app is keyless.
 *
 * For now this only registers a hidden "coming soon" element so the UI hook
 * exists (see AGENTS.md Phase 2 §7).
 */
export function initConcierge(): void {
  // Intentionally inert — no API keys in this phase.
}
